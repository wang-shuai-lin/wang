1.    声明合并是指 TypeScript 编译器会将名字相同的多个声明合并为一个声明，合并后的声明同时拥有多个声明的特性
2.Interface声明可以被覆盖
3.type只能声明一次,并不能被覆盖
4.TypeScript的所有声明概括起来，会创建这三种实体之一：**命名空间、类型**和**值**
5.命名空间的创建实际是创建一个对象，对象的属性是在命名空间里export导出的内容
6.类型的声明是创建一个类型并赋给一个名字
7.值的声明就是创建一个在JavaScript中可以使用的值