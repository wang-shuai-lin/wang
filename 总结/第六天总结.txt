1.泛型的概念：指在定义函数，接口或类的时候，不预先指定具体的类型，而在使用的时候再指定类型的一种特性。
2.泛型的标识符号<T>这里边的T不是固定的可以是A，B等，T是公认的。
3.fill填充数组的方法，将数据填充到其中，分别以不同的数据类型进行填充
4.泛型变量：当我们使用泛型的时候，必须要在任何地方处理的时候，把它当作泛型来进行处理，这意味着不是所有的类型都可以操作，也不是所有的类型都有属性
5.泛型函数类型：@1我们可以定义一个泛型函数类型，@2也可以定义一个接口类型的函数，
@3还可以把接口中的泛型变量提到最外层，这样在最内层都可以使用这个变量@4任意类型，但大多数情况下，我们是对特定类型进行处理的，泛型约束
6.使用泛型约束限制泛型变量，在泛型约束中适用类型参数，泛型约束 当我们使用了泛型时，就意味着这个类型是任意类型 但大多数情况下，我们是对特定类型进行处理的